import Custom from './Custom';
import Input from './Input';
import Textarea from './Textarea';

/**
 * `<Row.Custom />`, `<Row.Input/>` 등의 형태로 불러와 사용할 수 있습니다.
 */
export default {Custom, Input, Textarea};
